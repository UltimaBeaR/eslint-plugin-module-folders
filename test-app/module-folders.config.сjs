module.exports = {
  checks: [
    /**
     * @param {string} fromPath
     * @param {string} toPath
     */
    (fromPath, toPath) => {
      return "Нельзя так! " + fromPath + " - " + toPath + " !!";
    },
  ],
};
